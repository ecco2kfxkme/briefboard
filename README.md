# Briefboard

Локальная канбан‑доска для продуктовых и контентных задач: создание, фильтрация, завершение, удаление и экспорт в JSON. Состояние сохраняется в `localStorage`.

Стек: HTML, CSS, JavaScript. Зависимости и сборка не нужны — откройте `index.html`.
